# Core Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9
starlette>=0.46.0
anyio>=4.8.0

# Environment
python-dotenv==1.0.0

# HTTP + SSL
httpx>=0.28.0
requests>=2.31.0
certifi>=2024.2.2
urllib3>=2.0.0

# AI / ML — pinned for macOS MPS + numpy<2 compatibility
torch==2.5.1
numpy==1.26.4
Pillow==10.4.0
transformers==4.47.0
accelerate>=0.27.0
openai>=1.0.0
google-generativeai>=0.8.0
groq>=0.4.0

# HuggingFace Hub — must be >=0.27.0 for mirror endpoint support
huggingface_hub>=0.27.0
# Optional: 5x faster downloads (uncomment if you want to install it)
# hf_transfer>=0.1.4

# Semantic similarity
sentence-transformers>=2.2.2
scikit-learn>=1.4.0

# Video / Image Processing
ffmpeg-python==0.2.0
# Use headless to avoid cv2/av dylib conflict on macOS
opencv-python-headless==4.10.0.84

# MoviePy — use 1.0.3 which has stable macOS support
# (2.x has incomplete effect exports; FFmpeg fallback handles all effects)
moviepy==1.0.3
imageio==2.31.6
imageio-ffmpeg==0.4.9
proglog>=0.1.9

# Audio Processing
librosa>=0.10.0
scipy>=1.10.0

# Music Discovery
ytmusicapi>=1.11.0
yt-dlp>=2024.1.0

# TikTok Trends (optional)
TikTokApi>=6.1.0
playwright>=1.40.0

# Computer Vision (pin for numpy<2 compat)
scikit-image==0.24.0
albumentations==1.4.20
ultralytics==8.3.0

# Virality scoring
mediapipe>=0.10.0
pytrends>=4.9.0
pytesseract>=0.3.10
